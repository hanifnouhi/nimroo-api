export enum UserRole {
    Admin = 'admin',
    User = 'user',
    Moderator = 'moderator'
}

export enum UserStatus {
    Active = 'active',
    Suspender = 'suspended',
    Deleted = 'deleted'
}

export enum UserGoal {
    Language = 'language',
    General = 'general'
}

export enum UserProvider {
    Local = 'local',
    Google = 'google',
    Apple = 'apple',
    MicroSoft = 'microsoft',
    Facebook = 'facebook',
    LinkedIn = 'linkedin'
}